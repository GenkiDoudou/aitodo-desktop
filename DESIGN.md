# 桌面端视觉与交互设计规范

> **TodoMaster / 小柒 Todo Desktop** 重设计规范。  
> 来源：`设计/桌面端/设计.txt`（交互圣经）+ 三张高保真视觉稿。  
> 字段与页面清单见 [`docs/pages-overview.md`](./docs/pages-overview.md)；实现映射见该文档第五节。  
> **落地实施**：分阶段任务见 [`docs/redesign-implementation-plan.md`](./docs/redesign-implementation-plan.md)。

---

## 1. 设计目标

打造**效率优先的桌面 Todo 工具**：用户始终停留在工作上下文，快速捕获 → 收件箱整理 → 列表/看板/日历执行 → 完成 → 定时汇总。

| 维度 | 目标 |
|------|------|
| 布局 | 左导航固定 · 中工作区 · 右上下文（详情 Drawer） |
| 节奏 | 新建 ≤2 步；点任务不跳路由 |
| 信息密度 | 列表轻、详情全；字段存在但不默认全展开 |
| 一致性 | 列表 / 看板 / 日历 / 四象限操作同一 `Task` 实体 |
| 技术约束 | Vue 3 + Element Plus；优先 CSS 变量，避免散落硬编码色值 |

---

## 2. 核心交互原则

以下五条为**不可违背**的产品约束（摘自交互圣经）：

1. **任务不离开上下文** — 列表 → 点击任务 → 右侧 `TaskDetailPanel`（Drawer），禁止跳到独立详情路由。
2. **新建永远很快** — `Ctrl+N`、顶栏快捷输入、全局快捷捕获（`Ctrl+Shift+Space`）；支持自然语言解析后落收件箱。
3. **高级能力下沉** — AST 筛选、重复规则、汇总模板、AI、Webhook 等放在设置或二级入口；首屏只暴露任务、清单、日历、四象限。
4. **多视图同一数据** — 拖拽、勾选、改状态在各视图间即时同步。
5. **页面少、工作流多** — 详情 / 新建 / 编辑 / 清单编辑 / 视图编辑优先用 **Drawer / Dialog / Popover**，不增加路由。

### 2.1 文字方案 vs 图稿 — 统一口径

| 议题 | 采用方案 | 说明 |
|------|----------|------|
| 任务详情 | **右侧 Drawer**（宽 420–480px） | 图稿右侧详情面板形态；非全屏页、非居中大 Modal |
| 新建 / 编辑任务 | **右侧 Drawer**（非分步向导 Modal） | 图稿 `de269ef6` 的分步 Modal 仅作字段分组参考；默认单页可滚动，「安排 / 子任务 / 附件」折叠 |
| 消息中心 | **顶栏 Popover** | 不做独立消息页；侧栏可保留入口但行为一致 |
| 设置 | **左侧分类 + 右侧内容**（`#/settings`） | 13+ 子页分栏，禁止单页长表单 |
| 定时汇总编辑 | **分步向导**（基本信息 → 范围 → 内容 → 周期 → AI → 预览） | 复杂配置允许分步，与任务轻量新建区分 |

---

## 3. 应用布局

### 3.1 三栏工作台（主界面）

参考图稿 `8a3c6830-443f-4416-a22b-2d7a4e75206e.png`。

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 顶栏  Logo/工作区          全局搜索 (Ctrl+K)     +新建  🔔  主题  用户   │  h: 56px
├────────────┬──────────────────────────────────────────┬─────────────────┤
│            │  视图标题 + 计数                          │                 │
│  侧栏导航   │  筛选 · 排序 · 分组                      │  Task Drawer    │
│            ├──────────────────────────────────────────┤  (可选展开)      │
│  w: 240px  │  ＋ 添加任务...              Ctrl+N     │  w: 420–480px   │
│            │  ─────────────────────────────────────  │                 │
│            │  分组列表 / 看板 / 时间轴 / 四象限        │                 │
│            │                                          │                 │
└────────────┴──────────────────────────────────────────┴─────────────────┘
```

| 区域 | 宽度 | 行为 |
|------|------|------|
| 顶栏 | 全宽 × 56px | 固定；含全局搜索、主操作、通知 Popover |
| 侧栏 | 240px（可折叠至 64px 图标轨） | 固定；滚动区为导航项 |
| 工作区 | `flex: 1`；最小 480px | 随路由/视图切换内容 |
| 详情 Drawer | 420px 默认，最大 480px；可拖拽加宽 | 与列表并排，非遮罩全屏 |

**窗口最小尺寸建议**：1280 × 720。详情打开时列表 pane 不被完全挤没（列表 min-width ≥ 360px）。

### 3.2 顶栏

| 元素 | 规格 |
|------|------|
| 全局搜索 | 居中偏左，宽 480px；placeholder「搜索任务、清单、标签、描述…」；右侧角标 `Ctrl+K` |
| 新建任务 | `type="primary"` 按钮「+ 新建任务」，可带下拉（任务 / 从模板） |
| 通知 | 铃铛图标 + 数字角标（>9 显示 `9+`） |
| 主题 | 日/夜切换图标 |
| 用户 | 头像 32px + 昵称，下拉含设置入口 |

### 3.3 侧栏信息架构

```
工作台
├── 全部任务
├── 收件箱
├── 最近 7 天
├── 已完成
├── 垃圾桶
│
├── 我的视图（可展开）
│   ├── 本周重点
│   ├── 高优先级
│   ├── 未安排
│   └── + 自定义视图…
│
└── 我的清单（可展开）
    ├── 工作 / 项目 / 学习 / 生活 …
    └── 色点 + 名称 + 任务数

──────── 分隔线 ────────

效率工具
├── 日历
├── 四象限
└── 定时汇总

──────── 分隔线 ────────

系统
├── 消息（跳转或打开 Popover，与顶栏铃铛同源）
└── 设置
```

**侧栏行交互**：单击过滤 · 右键菜单（编辑 / 复制 / 新建任务 / 排序 / 删除）· 清单支持拖拽排序。

---

## 4. Design Tokens

> 图稿为白底 + 蓝主色（类 Ant Design）；实现时写入 `src/styles/desktop.scss` 或主题配置。  
> 与仓库根 [`DESIGN.md`](../DESIGN.md) 的关系：根文档为全项目 Element Plus 默认值；**本文档为桌面端重设计目标态**，落地时以本文为准并同步 CSS 变量。

### 4.1 色彩

#### 品牌与界面

| Token | 变量名（建议） | 值 | 用途 |
|-------|----------------|-----|------|
| 主色 | `--desktop-primary` | `#1677FF` | 主按钮、链接、选中态、焦点环 |
| 主色悬停 | `--desktop-primary-hover` | `#4096FF` | 按钮 hover |
| 主色浅底 | `--desktop-primary-light` | `#E6F4FF` | 选中导航项、搜索框 focus 底 |
| 页面背景 | `--desktop-bg` | `#FFFFFF` | 工作区底色 |
| 面板背景 | `--desktop-panel` | `#FAFAFA` | 列表区、卡片底 |
| 侧栏背景 | `--desktop-sidebar` | `#FFFFFF` | 侧栏；右边框分隔 |
| 边框 | `--desktop-border` | `#F0F0F0` | 分割线、输入框边框 |
| 悬停底 | `--desktop-hover` | `rgba(0,0,0,0.04)` | 行 hover、图标按钮 |
| 正文 | `--desktop-text` | `#141414` | 主文字 |
| 次要文字 | `--desktop-muted` | `#8C8C8C` | meta、占位符、辅助说明 |
| 禁用 | `--desktop-disabled` | `#BFBFBF` | 禁用文字/图标 |

#### 语义色

| Token | 值 | 用途 |
|-------|-----|------|
| 成功 | `#52C41A` | 完成态、进行中（可选） |
| 警告 | `#FAAD14` | 临期提醒 |
| 错误 / 危险 | `#FF4D4F` | 删除、P0、角标 |
| AI 入口 | `#0D9488` | 智能解析、AI 优化（与主色区分，沿用现网 `--desktop-ai`） |

#### 优先级（P0–P3）

数据层 `priority` 为 `1–4`，UI 同时展示**代号 + 语义标签**：

| 值 | 代号 | 语义标签 | 列表徽章（实心） | 浅底 tint（四象限等） | 场景 |
|----|------|----------|------------------|----------------------|------|
| 1 | P0 | 紧急重要 | 底 `#EF4444` / 字 `#FFF` | `#FEE2E2` / `#EF4444` | 列表、详情、搜索 |
| 2 | P1 | 重要 | `#F97316` / `#FFF` | `#FFEDD5` / `#F97316` | |
| 3 | P2 | 紧急 | `#EAB308` / `#FFF` | `#FEF9C3` / `#CA8A04` | |
| 4 | P3 | 普通 | `#9CA3AF` / `#FFF` | `#F3F4F6` / `#9CA3AF` | 无优先级时弱化 |

列表默认 `TaskPriorityBadge` 使用实心变体；四象限象限底色继续用浅底 tint token。

#### 清单预设色

侧栏色点、日历色条、看板 meta 共用：

| 清单 | 色值 |
|------|------|
| 工作 | `#1677FF` |
| 项目 | `#FA8C16` |
| 学习 | `#52C41A` |
| 生活 | `#722ED1` |

用户自定义清单从预设盘选取，保持饱和度和明度与上表一致。

### 4.2 字体

| Token | 值 |
|-------|-----|
| 正文字体 | `"Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", sans-serif` |
| 显示标题（可选） | 与正文同族；**重设计阶段统一 sans**，不再使用衬线 display 字体 |
| 基础字号 | 14px / line-height 22px |
| 侧栏项 | 14px |
| 列表任务标题 | 14px / medium (500) |
| 页面标题 | 20px / semibold (600) |
| 分组标题 | 13px / medium；颜色 `--desktop-muted` |
| Drawer 任务标题 | 18px / semibold |
| 角标 / 计数 | 12px |

### 4.3 间距与圆角

**间距基数 4px**，常用阶梯：`4 / 8 / 12 / 16 / 24 / 32`。

| Token | 值 |
|-------|-----|
| `--desktop-radius-sm` | 4px — 徽章、小标签 |
| `--desktop-radius-md` | 6px — 输入框、按钮、卡片 |
| `--desktop-radius-lg` | 8px — Drawer、Dialog |
| `--desktop-radius-pill` | 999px — 药丸按钮、搜索框 |

| 场景 | 内边距 |
|------|--------|
| 侧栏项 | 8px 12px |
| 任务行 | 10px 16px |
| 列表头 | 16px 24px |
| Drawer 内容区 | 16px 20px |
| 看板卡片 | 12px |

### 4.4 阴影与边框

重设计倾向**扁平**：默认无卡片阴影，用 1px 边框区分层级。

| 层级 | 样式 |
|------|------|
| 分割线 | `1px solid var(--desktop-border)` |
| Drawer | 左侧 `box-shadow: -2px 0 8px rgba(0,0,0,0.06)` |
| Popover / Dropdown | `0 6px 16px rgba(0,0,0,0.08)` |
| 拖拽中卡片 | `0 4px 12px rgba(0,0,0,0.12)` |

### 4.5 动效

| 类型 | 时长 | 曲线 |
|------|------|------|
| 微交互（hover、勾选） | 150ms | ease |
| 面板展开 / Drawer | 200ms | ease-out |
| 视图切换 | 200ms | ease-in-out |

---

## 5. 核心组件规范

### 5.1 任务列表行

参考图稿列表区 + 交互圣经第三节。

```
[○ 16px]  [P1]  设计 Todo 应用首页                    [☆]
          工作 · 今天 18:00 · 🔔 17:30
```

| 元素 | 规则 |
|------|------|
| 勾选框 | 圆形 16px；完成时显示 ✓ 并 strikethrough 标题 |
| 优先级徽章 | 药丸形，仅在有优先级时显示 |
| 标题 | 单行截断；选中行背景 `--desktop-primary-light` |
| Meta 行 | 清单名 · 截止时间 · 提醒图标；12px muted |
| 隐藏字段 | `recurrence`、`parentId`、`tags` 全文等仅在详情展示 |

**分组头**：`今天 · 9 个` — 13px muted，上下 margin 12px。

**快捷添加**：列表顶部全宽输入「＋ 添加任务…（支持自然语言）」；右侧灰字 `Ctrl+N`。

**列表头工具**：筛选 · 排序 · 分组（默认「截止日期」）· `…` 更多。

### 5.2 任务详情 Drawer（`TaskDetailPanel`）

对应 [`pages-overview.md`](./docs/pages-overview.md) 页面 8。

| 区块 | 内容 | 默认状态 |
|------|------|----------|
| 头栏 | ← 返回（窄屏）· 标题区 · 置顶/分享/更多/关闭 | 固定 |
| 状态条 | 优先级语义 · 清单 · 状态徽章 | 固定 |
| 标签行 | 可编辑 tag pills + `+` | 固定 |
| Tab | 详情 \| 动态 | 固定 |
| 描述 | Markdown 渲染 | 展开 |
| 进度 | 子任务进度条（有子任务时） | 展开 |
| 安排 | 截止 / 提醒 / 重复 | **折叠** |
| 子任务 | 清单 + 添加 | **折叠**（有子任务时可默认展开） |
| 附件 | 文件卡片 | **折叠** |
| 动态 | 活动记录时间线 | 在「动态」Tab |
| 底栏 | 删除（danger link）· 取消 · 保存 / 编辑 | 固定 |

宽度 **420px**（默认）～ **480px**（拖拽上限）；编辑态与查看态同一容器，避免跳转。

### 5.3 新建任务 Drawer

- 触发：`Ctrl+N`、顶栏「+ 新建任务」、列表底栏 `+`、看板列底 `+ 添加任务`。
- 字段顺序：任务名称 → 优先级 + 清单 → 标签 → 描述（Markdown）→ 折叠区「安排」「子任务」。
- 底栏：取消 · **创建任务**（primary）。
- 自然语言：在快捷添加框输入 `明天下午3点完成 API 文档 #工作 p1` → 解析后预填各字段 → 默认进**收件箱**。

### 5.4 看板（`TaskKanbanView`）

参考图稿 `c07ac7b1` 看板区。轻量，不像 Jira。

| 项 | 规格 |
|----|------|
| 列头 | 列名 + 计数；宽 280px 固定 |
| 列模式切换 | 状态下拉：状态 / 优先级 / 时间 / 标签 / 自定义 |
| 卡片 | 优先级角标 + 标题 + 清单 + 日期；padding 12px；圆角 6px；边框 1px |
| 列底 | `+ 添加任务` 文字按钮 |
| 拖拽 | 卡片跨列更新 `status` / `kanbanGroupId` / 分组字段 |

### 5.5 时间轴（`TaskTimelineView`）

- 左列固定任务名（含优先级），右列可横向滚动日期。
- 条带拖拽改 `startAt` / `dueAt`。
- 行高 40px；条带高度 24px；圆角 4px；颜色取清单色。

### 5.6 四象限（`QuadrantMatrixView`）

```
        重要 ↑
  P0 重要紧急  │  P1 重要不紧急
 ─────────────┼───────────── → 紧急
  P2 不重要紧急 │  P3 不重要不紧急
        不重要 ↓
```

- 象限背景：P0 `#FFF1F0` / P1 `#FFF7E6` / P2 `#FFFBE6` / P3 `#F9F9F9`。
- 每格内任务卡片可拖拽跨象限（改 `priority`）。
- 显示 **代号 + 语义**（见 §4.1）。

### 5.7 日历（`CalendarView`）

- 视图切换：年 / 月 / 周 / 日 / 自定义。
- 月格：日期数字 + 色点或色条任务摘要。
- 点击任务 → 打开 `TaskDetailPanel`，不跳页。
- 图例：底部清单颜色说明。

### 5.8 收件箱（`InboxView`）

原则：**先记下，后整理** — 不强迫分类/时间/标签。

```
快速记录（多行输入）
便签网格（WidgetNote 卡片）
待处理任务列表（triagedAt 为空）
```

点击任务 → 排优（设优先级/清单/时间）→ 可选离开收件箱。

### 5.9 已完成 / 垃圾桶

| 页面 | 要点 |
|------|------|
| 已完成 | 顶栏统计「本周完成 N 个」；时间筛选：今天 / 本周 / 本月 / 全部；按日分组 + 完成时刻 |
| 垃圾桶 | 极简列表；行内「恢复」「彻底删除」；清空需二次确认 |

### 5.10 自定义视图编辑器（`TaskViewEditor`）

可视化条件构建器（非原始 AST 表单）：

- 名称 · 布局（列表 / 看板 / 时间轴 / 四象限）
- 筛选行：`字段` + `运算符` + `值`，支持 AND 叠加
- 排序 · 分组
- 保存为侧栏「我的视图」

### 5.11 消息 Popover（`AppMessagePanel`）

宽 360px；Tab：通知 / 动态 / 待发送。  
不从侧栏打开全屏页；与顶栏铃铛同一组件。

### 5.12 设置（`SettingsView`）

左 nav 240px + 右 content。子页：数据存储、账号同步、通知、快捷键、大模型、提示词、导入导出、任务动态、窗口启动、工作日、外观、桌面挂件、关于等。

### 5.13 快捷捕获（`capture.html`）

全局 `Ctrl+Shift+Space` → 小浮窗 → 自然语言 → 预览字段 → Enter 保存 → **收件箱**。

### 5.14 桌面挂件（`widget.html`）

与主应用视觉区分：半透明/紧凑；形态：便签 / 四象限 / 视图；支持展开、迷你、贴边。

---

## 6. 弹层与路由策略

| 能力 | 容器 | 路由 |
|------|------|------|
| 任务详情 / 编辑 | 右侧 Drawer | 无（`HomeView` 内状态） |
| 新建任务 | 右侧 Drawer | 无 |
| 便签详情 | Drawer 或侧栏 panel | 无 |
| 清单编辑 | Dialog | 无 |
| 自定义视图编辑 | Dialog / 全屏 Drawer | 无 |
| 选择标签 / 模板 | Dialog | 无 |
| 消息 | Popover | 无 |
| 设置 | 独立布局 | `#/settings` |
| 日历 | 工作区全宽 | `#/calendar` 或 Home 子视图 |
| 快捷捕获 / 挂件 | 独立窗口 | `capture.html` / `widget.html` |

---

## 7. 快捷键

| 快捷键 | 行为 |
|--------|------|
| `Ctrl+N` | 新建任务 Drawer |
| `Ctrl+K` | 聚焦全局搜索 |
| `Ctrl+Shift+Space` | 快捷捕获浮窗 |
| `Escape` | 关闭 Drawer / Dialog（保存提示按现网逻辑） |

---

## 8. 工作流（用户旅程）

```mermaid
flowchart LR
  A[快捷捕获 / 收件箱输入] --> B[收件箱]
  B --> C[整理：清单 / 优先级 / 时间]
  C --> D[列表 / 看板 / 日历 / 四象限]
  D --> E[执行 / 详情编辑]
  E --> F[完成]
  F --> G[定时汇总 / 消息通知]
```

---

## 9. 与现网差异（迁移备忘）

| 项目 | 现网 (`desktop.scss` 默认) | 重设计目标 |
|------|---------------------------|------------|
| 背景基调 | 暖色 Claude（`#faf9f5`） | 白底中性灰（§4.1） |
| 标题字体 | Libre Baskerville 衬线 | 统一 Inter / 系统 sans |
| 主色 | Element `#409EFF` | `#1677FF`（可保留 EP 变量映射） |
| 详情 | 支持 sidebar / 其他样式配置 | 默认 sidebar Drawer |
| 新建任务 | 可能有独立流程 | 统一 Drawer + 自然语言 |

迁移时通过「外观」设置保留旧主题可选，但**新用户默认使用本规范**。

---

## 10. 设计资产索引

| 资产 | 路径 | 用途 |
|------|------|------|
| 交互圣经 | `设计/桌面端/设计.txt` | 原则、ASCII 线框、工作流 |
| 主界面效果图 | `设计/桌面端/8a3c6830-….png` | 三栏布局、列表行、详情 Drawer |
| 多视图合集 | `设计/桌面端/c07ac7b1-….png` | 看板、日历、四象限、收件箱 |
| 表单弹窗参考 | `设计/桌面端/de269ef6-….png` | 字段分组、子任务表、标签选择 |
| 页面字段清单 | `desktop/docs/pages-overview.md` | 数据字段、组件映射 |
| 样式变量 | `desktop/src/styles/desktop.scss` | 运行时 CSS 变量落点 |

---

## 11. AI 实现检查清单

生成或改版 UI 时自检：

- [ ] 是否保持三栏 / Drawer 不跳路由？
- [ ] 列表行是否只展示 title + priority + 少量 meta？
- [ ] 优先级是否同时显示代号与语义（P1 重要）？
- [ ] 颜色是否使用 `--desktop-*` 或 Element 主题变量？
- [ ] 间距是否落在 4px 网格上？
- [ ] 新建 / 详情是否用 Drawer 而非分步 Modal？
- [ ] 消息是否用 Popover？
- [ ] 看板 / 四象限 / 日历是否操作同一 Task store？

---

*文档版本：与 `设计/桌面端` 素材同步；有冲突以 §2.1 口径表为准。*
